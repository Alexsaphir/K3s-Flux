---
apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Provider
metadata:
  name: gitlab-provider
  namespace: flux-system
spec:
  type: gitlab
  address: https://gitlab.alexsaphir.com/Alexsaphir/K3s-Flux
  secretRef:
    name: gitlab-provider-token
