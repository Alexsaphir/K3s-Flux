---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres
  annotations:
    kyverno.io/ignore: "true"

spec:
  # imageName: ghcr.io/cloudnative-pg/postgresql
  imageName: ghcr.io/imusmanmalik/timescaledb-postgis:15-3.4@sha256:b5217294471d4dacc1383e7093787d822207557344520785395a5ff2a922e702

  instances: 2

  enableSuperuserAccess: true

  resources:
    requests:
      memory: "2Gi"

  storage:
    size: 10Gi
    storageClass: local-path

  monitoring:
    enablePodMonitor: true

  postgresql:
    parameters:
      max_connections: "800"
      shared_buffers: "512MB"
    shared_preload_libraries:
      - timescaledb

  bootstrap:
    recovery:
      source: &previousCluster postgres-v6

  backup:
    retentionPolicy: 30d
    barmanObjectStore:
      serverName: postgres-v7
      wal:
        compression: &compression bzip2
        maxParallel: &maxParallel 4

      destinationPath: &destinationPath 's3://cloudnativepg-backup/'
      endpointURL: &endpoint 'https://s3.alexsaphir.com'
      s3Credentials:
        accessKeyId:
          name: &s3Credentials-secret postgres-backup-secret
          key: &s3Credentials-accessKeyId-key ACCESS_KEY_ID
        secretAccessKey:
          name: *s3Credentials-secret
          key: &s3Credentials-secretAccessKey-key ACCESS_SECRET_KEY

  externalClusters:
    - name: *previousCluster
      barmanObjectStore:
        wal:
          compression: *compression
          maxParallel: *maxParallel
        destinationPath: *destinationPath
        endpointURL: *endpoint
        s3Credentials:
          accessKeyId:
            name: *s3Credentials-secret
            key: *s3Credentials-accessKeyId-key
          secretAccessKey:
            name: *s3Credentials-secret
            key: *s3Credentials-secretAccessKey-key
