---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app grafana
  namespace: &namespace observability
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
      app.kubernetes.io/component: *app

  dependsOn:
    - name: kube-prometheus-stack
      namespace: *namespace
    - name: victoria-logs
      namespace: *namespace
    - name: ext-postgres-operator
      namespace: database

  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: *app
      namespace: *namespace

  interval: 1h
  timeout: 5m
  retryInterval: 2m

  prune: true

  targetNamespace: *namespace
  path: cluster/infra/observability/grafana
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system
