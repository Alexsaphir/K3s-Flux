---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app kube-prometheus-stack
  namespace: &namespace observability
spec:
  dependsOn:
    - name: openebs
      namespace: openebs-system

  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: *app
      namespace: *namespace

  prune: true
  interval: 1h
  path: cluster/infra/observability/kube-prometheus-stack
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system
