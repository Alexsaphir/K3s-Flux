---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tailscale-operator
  namespace: tailscale-operator
spec:
  prune: true
  wait: false
  interval: 1h
  path: cluster/infra/tailscale/tailscale-operator
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system
