---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: spegel
  namespace: kube-system
spec:
  prune: false # never should be deleted
  wait: false
  interval: 1h
  path: cluster/infra/kube-system/spegel
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system
