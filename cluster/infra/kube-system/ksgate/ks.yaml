---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ksgate
  namespace: kube-system
spec:
  prune: true
  interval: 1h
  path: cluster/infra/kube-system/ksgate
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system
