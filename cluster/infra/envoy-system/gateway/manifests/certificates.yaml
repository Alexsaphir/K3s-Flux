---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-main
spec:
  dnsNames:
    - '${DOMAIN_NAME}'
    - '*.talos.${DOMAIN_NAME}'

  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer

  duration: 160h
  usages:
    - digital signature

  secretName: wildcard-main-tls
