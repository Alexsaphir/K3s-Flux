---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app timescaledb
  namespace: &namespace database
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app

  dependsOn:
    - name: cloudnative-pg
      namespace: cloudnative-pg

  healthChecks:
    - apiVersion: v1
      kind: Pod
      name: timescaledb-1
      namespace: *namespace

  interval: 1h
  timeout: 5m
  retryInterval: 2m

  prune: true

  targetNamespace: *namespace
  path: cluster/infra/database/timescaledb
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system
