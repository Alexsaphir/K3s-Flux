---
apiVersion: v1
kind: Service
metadata:
  name: s3-nas
  labels:
    app.kubernetes.io/name: s3-nas
    app.kubernetes.io/instance: s3-nas
spec:
  type: ExternalName
  externalName: tower.local
  ports:
    - name: http
      port: &port 9768
      protocol: TCP
      targetPort: *port
