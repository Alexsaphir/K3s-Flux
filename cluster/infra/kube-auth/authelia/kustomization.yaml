---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - external-secret.yaml
  - postgres.yaml
  - helm-release.yaml
  - referance-grant.yaml

configMapGenerator:
  - name: authelia-config-tpl
    files:
      - ./config/configuration.yaml
generatorOptions:
  disableNameSuffixHash: true
