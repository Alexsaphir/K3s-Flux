---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metallb
  namespace: metallb-system
spec:
  interval: 15m
  chart:
    spec:
      chart: metallb
      version: 0.13.9
      sourceRef:
        kind: HelmRepository
        name: metallb
        namespace: flux-charts
      interval: 15m

  install:
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3

  dependsOn:
    - name: kube-prometheus-stack
      namespace: flux-monitoring

  # https://github.com/metallb/metallb/blob/v0.13.7/charts/metallb/values.yaml
  values:
    crds:
      enabled: true

    prometheus:
      serviceAccount: 'prometheus-prometheus'
      namespace: 'flux-monitoring'

      podMonitor:
        enabled: true

      prometheusRule:
        enabled: true
