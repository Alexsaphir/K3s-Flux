---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: longhorn-frontend
spec:
  hostnames: [ 'longhorn.talos.${DOMAIN_NAME}' ]
  parentRefs:
    - name: main
      namespace: envoy-system
      sectionName: https
  rules:
    - backendRefs:
        - name: longhorn-frontend
          port: 80
