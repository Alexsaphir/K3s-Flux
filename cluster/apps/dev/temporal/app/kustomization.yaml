---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-release.yaml

configMapGenerator:
  - name: temporal-configmap
    files:
      - resources/dev-dynamic-config.yaml
generatorOptions:
  disableNameSuffixHash: true
