---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app gitlab-runner
  namespace: &namespace gitlab-runner
spec:
  postBuild:
    substitute:
      APP: *app

  prune: true
  interval: 1h
  path: cluster/apps/dev/gitlab-runner/app
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system
