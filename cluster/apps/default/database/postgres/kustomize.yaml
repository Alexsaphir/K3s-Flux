---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: &name cluster-example
  namespace: &namespace default
spec:
  interval: 10m
  prune: true
  timeout: 2m

  path: ./cluster/apps/default/database/postgres
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system

  healthChecks:
    - apiVersion: postgresql.cnpg.io/v1
      kind: Cluster
      name: *name
      namespace: *namespace
