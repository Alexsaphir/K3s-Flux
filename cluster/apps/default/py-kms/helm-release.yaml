---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: py-kms
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-charts
      interval: 15m

  maxHistory: 3
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false

  values:
    controller:
      type: statefulset
      annotations:
        reloader.stakater.com/auto: "true"

    image:
      repository: ghcr.io/py-kms-organization/py-kms
      tag: python3@sha256:95a73f03dfea0f9f4a234e5fa529a68614257108fd3b15110d6b80ab737e377e

    envFrom:
      - secretRef:
          name: py-kms-secret
    env:
      TIMEZONE: '${TIMEZONE}'
      IP: "::"

    service:
      main:
        ports:
          http:
            port: 8080
      kms:
        enabled: true
        type: LoadBalancer
        externalTrafficPolicy: Local
        annotations:
          metallb.universe.tf/loadBalancerIPs: ${LB_PY_KMS}
        ports:
          kms:
            port: 1688
            protocol: TCP

    probes:
      liveness:
        enabled: true
        custom: true
        spec:
          httpGet:
            path: /livez
            port: http
      readiness:
        enabled: true
        custom: true
        spec:
          httpGet:
            path: /readyz
            port: http
      startup:
        enabled: false

    persistence:
      db:
        enabled: true
        type: pvc
        existingClaim: py-kms-data
        mountPath: /home/py-kms/db

    ingress:
      main:
        enabled: true
        ingressClassName: traefik
        annotations:
          traefik.ingress.kubernetes.io/router.middlewares: network-system-authelia-auth@kubernetescrd
        hosts:
          - host: py-kms.${SECRET_DOMAIN_NAME}
            paths:
              - path: /
                pathType: Prefix
