---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: homepage
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden

  target:
    name: homepage-secret
    template:
      data:
        HOMEPAGE_VAR_LATITUDE: '{{ .HOMEPAGE_VAR_LATITUDE }}'
        HOMEPAGE_VAR_LONGITUDE: '{{ .HOMEPAGE_VAR_LONGITUDE }}'

        HOMEPAGE_VAR_UNIFI_URL: 'https://192.168.0.1'
        HOMEPAGE_VAR_UNIFI_KEY: '{{ .UNIFI_API_KEY }}'

  dataFrom:
    - extract:
        key: homepage
    - extract:
        key: unifi
