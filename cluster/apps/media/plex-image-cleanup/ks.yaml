---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: plex-image-cleanup
  namespace: media
spec:
  dependsOn:
    - name: plex

  prune: true
  interval: 1h
  path: cluster/apps/media/plex-image-cleanup
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system
