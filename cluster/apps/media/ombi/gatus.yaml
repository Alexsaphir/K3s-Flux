---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "ombi-gatus-ep"
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "ombi"
        group: Media
        url: "https://ombi.${SECRET_DOMAIN_NAME}/"
        interval: 1m
        client:
          dns-resolver: tcp://1.1.1.1:53
        conditions:
          - "[STATUS] == 200"
