---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "ombi-gatus-ep"
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "ombi"
        group: 'Apps - Media'
        url: "https://ombi.${DOMAIN_NAME}/"
        interval: 1m
        client:
          dns-resolver: tcp://1.1.1.1:53
        conditions:
          - "[STATUS] == 200"
