---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "tautulli-gatus-ep"
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "tautulli"
        group: Media
        url: "https://tautulli.${SECRET_DOMAIN_NAME}/"
        interval: 1m
        client:
          dns-resolver: tcp://1.1.1.1:53
        conditions:
          - "[STATUS] == 200"
