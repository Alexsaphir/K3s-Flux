---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app cleanarr
  namespace: media
spec:
  components:
    - ../../../../components/nfs-scaler
    - ../../../../components/volsync

  dependsOn:
    - name: plex

  postBuild:
    substitute:
      APP: *app
      PVC_NAME: *app
      VOLSYNC_CAPACITY: 5Gi

  prune: true
  interval: 1h
  path: cluster/apps/media/arr/cleanarr
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system
