---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: home-assistant
  namespace: home-automation
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.3.2
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-charts
      interval: 15m

  maxHistory: 3
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false

  values:
    image:
      repository: ghcr.io/onedr0p/home-assistant
      tag: 2023.4.1@sha256:c6cb5e49d7d1b9ed174e59494ec54b5ffe854ac3a9ec99d4eb75112e26b4d223

    podAnnotations:
      secret.reloader.stakater.com/reload: home-assistant-secret

    service:
      main:
        ports:
          http:
            port: 8123

    ingress:
      main:
        enabled: true
        ingressClassName: traefik
        hosts:
          - host: "ha.${SECRET_DOMAIN_NAME}"
            paths:
              - path: /
                pathType: Prefix

    env:
      TZ: "${CONFIG_TIMEZONE}"
      HASS_EXTERNAL_URL: "https://ha.${SECRET_DOMAIN_NAME}"
      POSTGRES_DB:
        valueFrom:
          secretKeyRef:
            name: database-home-assistant
            key: DATABASE_NAME

      HASS_POSTGRES_URL:
        valueFrom:
          secretKeyRef:
            name: database-home-assistant
            key: POSTGRES_URL
    envFrom:
      - secretRef:
          name: home-assistant-secret

    probes:
      liveness:
        enabled: false
      readiness:
        enabled: false
      startup:
        enabled: false
    podSecurityContext:
      runAsUser: 568
      runAsGroup: 568
      fsGroup: 568
      fsGroupChangePolicy: OnRootMismatch
    persistence:
      config:
        enabled: true
        existingClaim: home-assistant-config
