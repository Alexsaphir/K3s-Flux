---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zigbe2mqtt-data
  namespace: home-automation
  labels:
    app.kubernetes.io/name: &name zigbe2mqtt
    app.kubernetes.io/instance: *name
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
