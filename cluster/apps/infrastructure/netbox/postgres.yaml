---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: netbox-postgres
  namespace: default
  annotations:
    kyverno.io/ignore: "true"

spec:
  instances: 1
  storage:
    size: 5Gi
    storageClass: longhorn
