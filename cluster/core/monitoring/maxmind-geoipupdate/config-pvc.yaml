---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: maxmind-geoipupdate-config-v1
  namespace: flux-monitoring
  labels:
    excluded_from_alerts: "true"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
  volumeName: maxmind-geoipupdate-config-v1
