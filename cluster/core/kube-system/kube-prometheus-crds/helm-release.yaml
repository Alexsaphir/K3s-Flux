---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kube-prometheus-stack-crds
  namespace: kube-system
spec:
  interval: 15m
  chart:
    spec:
      chart: kube-prometheus-stack-crds
      version: 41.7.3
      sourceRef:
        kind: HelmRepository
        name: morremeyer-chart
        namespace: flux-charts
      interval: 15m
