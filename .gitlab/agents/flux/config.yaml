---
ci_access:
  projects:
    - id: Homelab/K3s-Flux

user_access:
  access_as:
    agent: { }

  projects:
    - id: Homelab/K3s-Flux

remote_development:
  enabled: true
  dns_zone: "workspace.k3s.alexsaphir.com"
  gitlab_workspaces_proxy:
    namespace: gitlab-workspace
