[tool.poetry]
name = "k3s-flux"
version = "0.1.0"
description = ""
authors = ["Alexandre Mechineau <git@alexsaphir.com>"]
readme = "README.md"

packages = []

[tool.poetry.dependencies]
python = ">=3.10,<3.12"
pre-commit = "^3.6.0"

prettier = "^0.0.7"
yamllint = "^1.33.0"

[tool.poetry.group.ansible.dependencies]
ansible = "^9.1.0"
ansible-inventory-grapher = "^2.5.0"

jmespath = "^1.0.1"
netaddr = "^0.9.0"

[tool.poetry.group.ci.dependencies]
flux-local = "^4.1.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
