---
machine:
  kubelet:
    extraMounts:
      - destination: /var/lib/local
        type: bind
        source: /var/lib/local
        options:
          - bind
          - rshared
          - rw
      - destination: /var/lib/longhorn
        type: bind
        source: /var/lib/longhorn
        options:
          - bind
          - rshared
          - rw
