---
machine:
  kubelet:
    extraMounts:
      - destination: /var/storage/local
        type: bind
        source: /var/storage/local
        options:
          - bind
          - rshared
          - rw
